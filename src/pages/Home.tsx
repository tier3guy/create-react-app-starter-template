const Home = () => {
    return (
        <div className="bg-slate-800 h-screen w-screen grid place-content-center">
            <h1>
                Hi, welcome to avinash templates / react / typescript / tailwind
            </h1>
        </div>
    );
};

export default Home;
